#!/bin/bash
# This script will run .desktop files

SCRIPT_DESC="This script will run .desktop files"
commons_dir="/usr/local/bin/common"

`grep '^Exec' $1 | tail -1 | sed 's/^Exec=//' | sed 's/%.//' | sed 's/^"//g' | sed 's/" *$//g'` &
